// 1. Declare uma variavel de nome weight
    //    let weight

//2. Que tipo de dado é a variavel acima?
    //  Console.log(typeof weight)

/*
   3. Declare uma variavel e atribua valories para cada um dos dados:
        *name: String
        *age: Number (integer)
        *stars: Number (float)
        *isSubscribed: Boolean
*/

    //  let name= "Mayk"
    //  let age = 23
    //  let weight = 74.8
    //  let isSubscribed=true

/*
    4.A Variavel student abaixo é de que tipo de dados?
    4.1 Atribua a ela as mesmas propriedades e valores do exercicio 3.
    4.2 Mostre no console a seguinte mensagem:
            <name> de idade <age> peso <weight> kg
            Atencao substitua <name><age>e<weight> pelos valores de cada propriedade do objeto.
*/
 /*   let student = {
         name: "Mayk",
         age: 23,
         weight: 74.8,
         isSubscribed: true,
    }
    
    console.log(`${student.name} de idade ${student.age} pesa ${student.weight}kg.`student)

    */
    /*
        5.Declare uma variavel do tipo Array, de nome students e atribua a ela nenhum valor, ou seja, somente o Array vazio
    */

        // let students=[]

    /* 
        6. Reatribua valor para a variavel acima, colocando dentro dela o objeto student da questao 4. (Nao copiar e coloar o objeto,
            mas usar o objeto criado e inserior ele no array)
    */

        //students=[
        //      student
        // ]
        // console.log(students)

     /*
        7.Coloque no console o valor da posicao zero do Array acima.
     */
     // console.log(students[0])

    /*
    8. Crie um novo student e coloque na posição 1 do Array students.
    */

    /*
    const john = {
        name: "John",
        age: 23,
        weight: 74.8,
        isSubscribed: true,
    }

    students=[
        student,
        john
    ]
    console.log(students)
    */

    /*
        9.Sem Rodar o codigo responda qual é a resposta do codigo abaixo e por que? Apos sua resposta, rode o codigo e veja se voce acertou.

        console.log(a)
        var a=1  (Undefined)

        
    */
